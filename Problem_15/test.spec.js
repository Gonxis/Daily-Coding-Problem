const selectRandomStream = require('./Problem_15');

describe('Functions', () => {
    test('selectRandomStream function exists', () => {
        expect(typeof selectRandomStream).toBe('function');
    });
});